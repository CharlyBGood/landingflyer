# _legacy

Archived components that are no longer in use but kept for reference.
Do not import from this folder. Candidates for deletion in a later cleanup PR.

- `GrapesJsEditor.jsx` — superseded by iframe editor
- `TemplateEditor*.jsx` — unused template UI experiments
- `TailwindPreview.jsx` — stub wrapper around `PreviewEditorPage`
- `PreviewEditorPage.jsx`, `PreviewHeader.jsx`, `MobileActionBar.jsx` — parallel editor route that was never wired to `main.jsx`
- `BackgroundImageButton.jsx`, `useBackgroundButtons.js` — pre-iframe image editing approach
- `*.md` — internal fix logs, kept for historical context
